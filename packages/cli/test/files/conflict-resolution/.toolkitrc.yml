plugins:
  - '../../../../webpack'
  - '../../../../babel'
  - '../../../../circleci' # for build:ci hook
  - '../../../../heroku' # for build:remote hook and build:local via npm plugin

options:

hooks:
  build:local:
    - WebpackDevelopment
    - BabelDevelopment
  build:ci:
    - WebpackProduction
    - BabelProduction
  build:remote:
    - WebpackProduction
    - BabelProduction
