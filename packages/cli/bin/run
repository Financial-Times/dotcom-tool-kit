#!/usr/bin/env node

const { load, runCommand } = require('../lib')

const [command, ...argv] = process.argv.slice(2)

load().then(() => {
   runCommand(command, argv).then(
      console.log,
      console.error // TODO real error handling
   )
})
