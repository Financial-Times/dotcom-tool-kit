{
  "name": "@dotcom-tool-kit/heroku-postbuild",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "if is-installing-package; then ./bin/run heroku:install; else npm run build; fi",
    "build": "tsc -b"
  },
  "files": [
    "/lib"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@financial-times/package-json": "^3.0.0",
    "@oclif/command": "^1.8.0",
    "@quarterto/is-installing-package": "^1.5.0"
  },
  "oclif": {
    "commands": "./lib/commands",
    "topics": {
      "heroku": {
        "description": "commands to do things for heroku"
      }
    },
    "hooks": {
      "init": "./lib"
    }
  },
  "devDependencies": {
    "typescript": "^4.2.2"
  }
}
