description: >
  Sample description
# What will this job do?
# Descriptions should be short, simple, and clear.

executor: node/default

steps:
  - attach-workspace
  - run:
      name: Smoke tests
      command: npx dotcom-tool-kit lifecycle test:remote
