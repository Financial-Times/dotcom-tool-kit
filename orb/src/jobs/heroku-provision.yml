executor:
  name: node/default
  tag: "12.22"

steps:
  - attach-workspace
  - run:
      name: Create and test Heroku review app
      command: npx dotcom-tool-kit deploy:review
  - persist-workspace
