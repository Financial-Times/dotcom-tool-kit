parameters:
  node-version:
    default: "12.22"
    type: string

executor:
  name: node/default
  tag: <<parameters.node-version>>

steps:
  - checkout
  - node/install-packages
  - persist-workspace
