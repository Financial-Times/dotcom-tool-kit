parameters:
  executor:
    default: default
    type: executor

executor: << parameters.executor >>

steps:
  - attach-workspace
  - run:
      name: Smoke tests
      command: npx dotcom-tool-kit test:review
