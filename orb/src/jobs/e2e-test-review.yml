parameters:
  node-version:
    default: "16.14-browsers"
    type: string

executor:
  name: node/default
  tag: <<parameters.node-version>>

steps:
  - attach-workspace
  - run:
      name: Smoke tests
      command: npx dotcom-tool-kit test:review
