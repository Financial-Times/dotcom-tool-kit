plugins:
  - '@dotcom-tool-kit/package-json-hook'

tasks:
  NpmPrune: './lib/tasks/prune.js'
  NpmPublish: './lib/tasks/publish.js'

options:
  hooks:
    - PackageJson:
        scripts:
          build: 'build:local'
          test: 'test:local'
          start: 'run:local'

version: 2
