workflows:
  tool-kit:
    jobs:
      - test-job:
          requires:
            - another-job
          executor: node
          filters:
            tags:
              only: /^v\d+\.\d+\.\d+(-.+)?/
  nightly:
    jobs:
      - test-job:
          requires:
            - another-job
          executor: node
