workflows:
  tool-kit:
    jobs:
      - test-job:
          name: test-job-node
          requires:
            - another-job-node
          executor: node
          filters:
            tags:
              only: /^v\d+\.\d+\.\d+(-.+)?/
  nightly:
    jobs:
      - test-job:
          name: test-job-node
          requires:
            - another-job-node
          executor: node
